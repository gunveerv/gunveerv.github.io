import { Toolbar } from '@mui/material';

function Bar() {
  return (
    <div className="Bar">
      <Toolbar>
        <h2>
          Bar
        </h2>
      </Toolbar>
    </div>
  );
}

export default Bar;
